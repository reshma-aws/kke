Task:
====

" Connect from jump server to App Server without entering passwords

" Generate key pair 
$ ssh-keygen -t rsa 

" Copy the key to App Server 01
$ ssh-copy-id tony@stapp01

"Enter tony's password 

The keypair will be copied to the App Server and the user thor can login without password